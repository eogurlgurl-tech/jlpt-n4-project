"use strict";

/**
 * ============================================================
 * Japanese Master Road
 * Application Configuration
 * Version : 3.0.0
 * ============================================================
 */

window.APP_CONFIG = Object.freeze({
    // ========================================================
    // Application
    // ========================================================
    APP: Object.freeze({
        NAME: "Japanese Master Road",
        VERSION: "3.0.0",
        AUTHOR: "JMR Team"
    }),

    // ========================================================
    // Learning
    // ========================================================
    LEARNING: Object.freeze({
        DEFAULT_LEVEL: "N5",
        DAILY_GOAL: 20
    }),

    // ========================================================
    // Theme
    // ========================================================
    THEME: Object.freeze({
        DEFAULT: "light"
    }),

    // ========================================================
    // Storage
    // ========================================================
    STORAGE: Object.freeze({
        PREFIX: "jmr"
    }),

    // ========================================================
    // Debug
    // ========================================================
    DEBUG: Object.freeze({
        ENABLED: false
    })
});